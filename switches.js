const telldus = require('telldus')

/*
telldus.getDevices(function(err,devices) {
  if ( err ) {
    console.log('Error: ' + err);
  } else {
    // A list of all configured devices is returned
    console.log('\n\nSwitches:', devices);
  }
});
*/

/*
telldus.turnOn(116, (err) => {
  if (err) {
    console.log('Error:', err)
  } else {
    console.log('116 turned on')
  }
})

setTimeout(() => {
  telldus.turnOff(116, (err) => {
    if (err) {
      console.log('Error:', err)
    } else {
      console.log('116 turned off')
    }
  })
}, 3000)
*/
